version: '3'
services:
  anvil:
    image: "nwnxee/unified:${NWNX_VERSION}"
    env_file:
      - ${PWD-.}/config/nwserver.env
    volumes:
      - ${PWD-.}/../../../NWN.Anvil/bin/Release:/nwn/anvil
      - ${PWD-.}/NWN.Anvil.Tests:/nwn/run/anvil/Plugins/NWN.Anvil.Tests
      - ${PWD-.}/results:/nwn/run/anvil/PluginData/NWN.Anvil.Tests
